import React from 'react';

import './PreviewHint.scss';

const PreviewHint = (props) => {

    return (
        <div className='hint'>
            Zvolte sloupec/sloupce, jejichž kombinace musí být unikátní a zvolte tlačítko další.

        </div>

    );
};

export default PreviewHint;
